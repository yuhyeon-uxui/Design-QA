# 트러블슈팅 기록 (Troubleshooting Log)

## 1. Firebase 실시간 연동 중 Local UI State 전이 현상 (2026-06-01)

### 📌 문제 상황 (Issue)
- 다수의 사용자가 QA 보드(ScreenQA)에 접속 중일 때, 한 사용자가 특정 핀(이슈)을 클릭하면 접속 중인 다른 모든 사용자의 화면에서도 강제로 해당 핀 상세 보기로 전환되는 현상 발생.
- 사용자 경험 상 마치 여러 명이 하나의 마우스를 공유하는 것과 같은 오작동(버그)으로 인지됨.

### 🔍 원인 분석 (Cause)
- QA 화면에 필요한 모든 데이터(이미지 URL, 핀 좌표, 코멘트 목록 등)를 하나의 객체인 `ScreenDeviceState`로 묶어서 Firebase Realtime Database(Firestore)에 동기화하도록 설계됨.
- 이 과정에서 핀에 대한 공통 데이터뿐만 아니라, **"현재 브라우저에서 사용자가 클릭해서 보고 있는 핀의 ID(`activePinId`)"** 값까지 `ScreenDeviceState`에 포함되어 있었음.
- 한 사용자가 핀을 클릭하면 로컬 UI 상태가 변경될 뿐만 아니라, 이 상태가 서버의 `screens` 데이터 전체 업데이트(updateActiveDeviceState)를 트리거하여 Firebase에 반영됨.
- Firebase의 `onSnapshot` 이벤트가 실시간으로 다른 클라이언트로 해당 변경 사항을 푸시하면서, 다른 사용자의 로컬 `activePinId`마저 강제로 덮어씌워버림.

### 🛠 해결 방법 (Resolution)
- **로컬 UI 상태 분리 (Decoupling UI State from Shared State):**
  - `ScreenDeviceState` 인터페이스 및 Firestore 동기화 대상 객체에서 `activePinId` 필드를 완전히 제거함.
  - `page.tsx` 내부에서 `activePinId`를 서버 연동과 무관한 순수 로컬 React State(`useState`)로 분리함.
- **상태 초기화 로직 보완:**
  - 기기 변경(PC <-> Mobile) 또는 화면 변경 시 이전 화면의 `activePinId`가 잔존하지 않도록 `useEffect`를 사용해 로컬 상태를 깔끔하게 초기화(`null`) 하도록 개선.

### 💡 교훈 및 향후 개선점 (Takeaway)
- 상태 관리를 설계할 때, "여러 사용자가 동일하게 공유해야 하는 서버 상태(Server State)"와 "사용자 개인의 화면에서 일시적으로 유지되는 클라이언트 상태(Client/Local UI State)"를 명확히 구분해야 함.
- 스크롤 위치, 모달 창 오픈 여부, 현재 포커스된 항목(active item) 등은 절대로 서버 동기화 객체에 포함되어서는 안 됨.

---

## 2. Mac OS 환경 한글 입력(IME) 시 Enter 키 중복 전송 버그 (2026-06-01)

### 📌 문제 상황 (Issue)
- Mac OS (주로 Chrome, Safari 환경)에서 코멘트를 작성하다가 한글 문장 끝에서 바로 `Enter` 키를 누르면, 마지막 글자가 한 번 더 반복되어 전송되는 현상 발생 (예: "어떻게된거야" -> "어떻게된거야ㅑ").

### 🔍 원인 분석 (Cause)
- 한글은 자음과 모음이 결합하여 완성되는 '조합형 언어'임. 글자를 타이핑하는 동안 브라우저는 해당 글자를 "조합 중(Composing)" 상태로 인식함.
- 조합을 마치기 위해 `Enter` 키를 누를 때, 브라우저는 1) 글자 조합을 완료하는 이벤트와 2) 키보드의 일반 `Enter` 이벤트(keydown)를 거의 동시에 연달아 발생시킴.
- React의 `onKeyDown` 핸들러가 전송 로직(`handleAddComment`)을 실행하는 타이밍과 글자 조합 확정 타이밍이 겹치면서 문자가 중복으로 달라붙거나 이벤트가 두 번 실행됨.

### 🛠 해결 방법 (Resolution)
- **조합 중 상태(isComposing) 체크 및 방어 로직 추가:**
  - 코멘트 등록 창과 수정 창의 `onKeyDown` 핸들러 최상단에 `e.nativeEvent.isComposing` 속성을 확인하는 코드를 추가.
  - 사용자가 한글을 조합 중일 때 들어오는 `Enter` 입력은 무시(`return`)하도록 처리하여, 조합이 완전히 확정된 후의 입력만 전송 로직을 타도록 수정함.
  
  ```tsx
  onKeyDown={(e) => {
    if (e.nativeEvent.isComposing) return; // 한글 조합 중일 때는 전송 이벤트 무시
    if (e.key === 'Enter') handleAddComment();
  }}
  ```

### 💡 교훈 및 향후 개선점 (Takeaway)
- 한글, 일본어, 중국어 등 다국어 IME를 사용하는 웹 환경에서 키보드 이벤트를 이용해 엔터 키 전송을 구현할 때는, 반드시 `isComposing` 속성을 체크하는 것을 기본 원칙(Best Practice)으로 삼아야 함.


## [2026.08.27] ȯ�� ���� �� ������ ���� ����

### 1. Supabase �̸��� Rate Limit ����
- **����**: ȸ������ �� �̸��� ���� �ѵ� �ʰ� ���� �߻�.
- **����**: Supabase ���� Ƽ���� �⺻ SMTP ������ �׽�Ʈ������ �ð��� 3�� ������ ����.
- **�ذ�/����**: �̸��� ���� ������ �Ͻ������� ��Ȱ��ȭ�Ͽ���, ���� �ʿ� �� ������Ʈ ������ Ŀ���� Gmail SMTP ������ ���� �Ϸ� 500�� �ѵ��� �ذ��ϱ�� ��å ���� (��й�ȣ ã�� ����� ��뼺 ��� ������ Ŀ�� �����Ͱ� ���� �ʱ�ȭ�ϴ� ������� ����).

### 2. Gemini API ȣ�� �𵨸� ����
- **����**: GA4 ��� ��ú��忡�� AI ����Ʈ �ڵ� ���� �� 404 Model Not Found ���� �߻�.
- **����**: �ڵ� �� �������� �ʴ� gemini-2.5-flash �𵨸��� �ϵ��ڵ� �Ǿ� �־���.
- **�ذ�**: �ùٸ� �𵨸��� gemini-1.5-flash�� �����Ͽ� ���� ���� Ȯ��.

### 3. ������ ���� �׺���̼� ���� ����
- **����**: ���� �׺���̼� �ǿ��� ������ �ý��� ���ڵ���� Ŭ������ ����.
- **����**: ������ �ý��� ���� ���¸� �����ϴ� onOpenChange �� ���� ���� �����Ǿ� Ŭ�� �̺�Ʈ�� ���� ����.
- **�ذ�**: AdminLayout.tsx ���Ͽ� isDesignSystemOpen ���¸� �߰��Ͽ� ���ڵ���� ���������� ���� ���� �� �ְ� ����.
